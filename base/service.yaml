# Service for Book Microservice

apiVersion: v1
kind: Service
metadata:
  name: book-service
  namespace: book-service
  labels:
    app.kubernetes.io/name:      book-service
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app: book-service
  ports:
    - name:       http
      port:       80
      targetPort: http
      protocol:   TCP

# ---

# apiVersion: v1
# kind: Service
# metadata:
#   name: book-service-np
#   namespace: book-service
#   labels:
#     app.kubernetes.io/name:      book-service
#     app.kubernetes.io/component: server-nodeport
# spec:
#   type: NodePort
#   selector:
#     app: book-service   # Must match pod template labels in Deployment
#   ports:
#     - name: http
#       port: 80          # Cluster-internal port clients connect to
#       targetPort: http        # Named port on the pod (containerPort: 3000)
#       protocol: TCP
#       nodePort: 30082    # Optional fixed node port (30000-327